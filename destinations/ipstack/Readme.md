# IPStack Custom Destination Function

This function forwards events to IPStack and writes back enriched data to another Segment source.

## Settings

- `apiKey` (string) ([IPStack API key](https://ipstack.com/))
